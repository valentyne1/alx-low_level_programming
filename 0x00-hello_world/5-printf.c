#include <stdio.h>

/**
 * main - printf "with proper grammar, but the outcome is a
 *  piece of art,
 *  Return: Always 0 (Success)
 */

int main(void)
{
	char c = "with proper grammar, but the outcome is a piece of art,";

	printf("\n%c", c";

			return (0);
}
